{
    admin off
    auto_https off
}

# Nest's public Caddy terminates TLS and forwards the registered default domain
# to this per-container Unix socket.
http://sharkingstudios.hackclub.app {
    bind unix//root/.sharkingstudios.hackclub.app.webserver.sock
    reverse_proxy 127.0.0.1:8000
}
